# per-rule pressure override (uncomment to override pressure.conf / conf.bfd)
# PRESSURE_WEIGHT="3"
# PRESSURE_TRIP="15"

# uncomment to disable alerting for this rule
# SKIP_ALERT="1"

# uncomment to route alerts for this rule to a different address
# RULE_EMAIL=""

# file must exist for rule to be active
PREREQ="/usr/sbin/xrdp-sesman"

if [ -f "$PREREQ" ]; then
	PORTS="3389"
	LOG_FILE="/var/log/xrdp-sesman.log"
	LOG_TAG="xrdp"

	## XRDP
	MATCHED_HOSTS=$(_rule_tlog "$LOG_FILE" "$LOG_TAG" | extract_hosts \
		"sesman.*AUTHFAIL.*ip=<HOST>" \
		"sesman.*pam_authenticate failed.* <HOST>")
fi
