# per-rule threshold override (uncomment to override thresholds.conf / conf.bfd)
# TRIG="5"

# uncomment to disable alerting for this rule
# SKIP_ALERT="1"

# uncomment to route alerts for this rule to a different address
# RULE_EMAIL=""

# file must exist for rule to be active
REQ="/usr/local/psa/admin/bin/panel"

if [ -f "$REQ" ]; then
	PORTS="8443,8880"
	LP="/var/log/plesk/panel.log"
	TLOG_TF="plesk"

	## Plesk panel authentication failures
	ARG_VAL=$(_rule_tlog "$LP" "$TLOG_TF" | extract_hosts \
		"Failed login attempt .* from IP <HOST>")
fi
