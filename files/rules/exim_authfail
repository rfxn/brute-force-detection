# per-rule pressure override (uncomment to override pressure.conf / conf.bfd)
# PRESSURE_WEIGHT="2"
# PRESSURE_TRIP="20"

# uncomment to disable alerting for this rule
# SKIP_ALERT="1"

# uncomment to route alerts for this rule to a different address
# RULE_EMAIL=""

# file must exist for rule to be active
REQ="/usr/sbin/exim"

if [ -f "$REQ" ]; then
	PORTS="25,26,465"
	LP="/var/log/exim_mainlog"
	TLOG_TF="exim_authfail"

	## EXIM SMTP Auth failures
	ARG_VAL=$(_rule_tlog "$LP" "$TLOG_TF" | extract_hosts \
		"login authenticator failed.*\[<HOST>\]" \
		"dovecot_.*authenticator failed.*\[<HOST>\]" \
		"SMTP protocol synchronization error.*\[<HOST>\]")
fi
