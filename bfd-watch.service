[Unit]
Description=Brute Force Detection (BFD) - Watch Mode
After=network.target
Conflicts=bfd.timer

[Service]
Type=simple
ExecStart=/usr/local/sbin/bfd --watch
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
